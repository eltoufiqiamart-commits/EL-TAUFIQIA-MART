"use client";
import {useEffect,useState} from "react"; import Link from "next/link";
export default function AdminProducts(){const [products,setProducts]=useState<any[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);const r=await fetch(`/api/admin/products?q=${encodeURIComponent(q)}`);const d=await r.json();setProducts(d.products||[]);setLoading(false)};useEffect(()=>{load()},[]);const deactivate=async(id:string)=>{if(!confirm('تعطيل المنتج؟'))return;await fetch(`/api/admin/products/${id}`,{method:'DELETE'});load()};return <div className="p-4 sm:p-6" dir="rtl"><div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3 mb-5"><div><h1 className="text-2xl font-black">المنتجات</h1><p className="text-sm text-gray-500 mt-1">إدارة منتجات المتجر وصورها</p></div><Link href="/admin/products/new" className="bg-[#1565C0] text-white px-4 py-2.5 rounded-lg text-sm font-bold text-center">+ إضافة منتج</Link></div><div className="bg-white border rounded-xl p-3 mb-4 flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="ابحث بالاسم أو رقم القطعة" className="flex-1 border rounded-lg px-3 py-2 text-sm"/><button onClick={load} className="px-4 rounded-lg bg-gray-100 text-sm">بحث</button></div>{loading?<div>جاري التحميل...</div>:<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{products.map(p=><div key={p.id} className="bg-white border rounded-xl overflow-hidden"><div className="aspect-[4/3] bg-gray-50">{p.mainImageUrl&&<img src={p.mainImageUrl} className="w-full h-full object-cover"/>}</div><div className="p-3"><h2 className="font-bold line-clamp-2">{p.nameAr}</h2><p className="text-xs text-gray-500 mt-1">{p.categoryNameAr||'بدون تصنيف'} {p.brandNameAr?`· ${p.brandNameAr}`:''}</p><div className="flex justify-between mt-3 text-sm"><span>{p.price} ج.م</span><span className={p.isActive?'text-green-600':'text-red-500'}>{p.isActive?'متاح':'متوقف'}</span></div><div className="flex gap-2 mt-3"><Link href={`/admin/products/${p.id}/edit`} className="flex-1 text-center border rounded-lg py-2 text-sm">تعديل</Link><button onClick={()=>deactivate(p.id)} className="border border-red-200 text-red-600 rounded-lg px-3 text-sm">تعطيل</button></div></div></div>)}</div>}</div>}
